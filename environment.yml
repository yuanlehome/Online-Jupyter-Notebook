name: binder-base
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  - jupyterlab
  - ipython
  - numpy
  - scipy
  - pandas
  - pillow
  - requests
  - aiohttp
  - tqdm
  - packaging
  - psutil
  - pyzmq>=25.1.2
  - orjson
  - msgspec
  - pydantic
  - python-multipart
  - prometheus_client>=0.20.0
  - uvicorn
  - uvloop
  - sentencepiece
  - tabulate
  # 构建/编译相关（很多 pip 包会用到）
  - setuptools>=61.0
  - wheel
  - ninja
  - pip:
      # ===== 你的清单（尽量原样保留版本） =====
      - anthropic>=0.20.0
      - blobfile==3.0.0
      - build
      - compressed-tensors
      - datasets
      - einops
      - fastapi
      - gguf
      - hf_transfer
      - huggingface_hub
      - interegular
      - modelscope
      - openai-harmony==0.0.4
      - openai==2.6.1
      - outlines
      - partial_json_parser
      - py-spy
      - pybase64
      - setproctitle
      - tiktoken
      - timm==1.0.16
      - torch==2.9.0
      - torchao==0.14.1
      - torchaudio==2.9.0
      - torchvision==0.24.0
      - transformers==4.57.1
      - triton==3.5.0
      - xgrammar==0.1.27
